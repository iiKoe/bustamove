package com.group66.game.helpers;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import com.badlogic.gdx.graphics.g2d.SpriteBatch;

public class TextDrawerTest {
    /*
    @Test
    public void creationTest() {
        new TextDrawer();
    }
 
    @Test
    public void drawTest() {
        TextDrawer drawer = new TextDrawer();
        SpriteBatch batch = new SpriteBatch();
        
        assertEquals(0, batch.maxSpritesInBatch);
        
        drawer.draw(batch, "Text", 0, 0);
        assertEquals(1, batch.maxSpritesInBatch);
        
        drawer.drawScore(batch, 10);
        assertEquals(2, batch.maxSpritesInBatch);
    }
    */
}
